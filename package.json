{
  "name": "brave-prompt-palette",
  "version": "1.0.0",
  "description": "A Chromium browser extension for managing and using prompts for use with AI.",
  "main": "js/background.js",
  "author": "Alexander Rollison",
  "license": "MIT",
  "scripts": {
    "start": "echo 'Starting extension...' && exit 0",
    "build": "tsc",
    "dev": "tsc --watch",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5.7.3"
  }
}
